% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-fs_path.R
\name{opts_fs_path}
\alias{opts_fs_path}
\title{Constructive options for class 'fs_path'}
\usage{
opts_fs_path(constructor = c("path", "as_fs_path", "next"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object, see Details section.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_fs_path>
}
\description{
These options will be used on objects of class 'fs_path'.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"path"} (default): Build the object using \code{fs::path()} on a character
vector.
\item \code{"as_fs_path"} : Build the object using \code{fs::as_fs_path()} on a character
vector.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried.
}

Both \code{fs::path()} and \code{fs::as_fs_path()} tidy their input, so paths that are
not tidy (e.g. \code{"a//b/"}) are always constructed with the \code{"next"} constructor.
}
