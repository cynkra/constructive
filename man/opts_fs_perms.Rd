% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-fs_perms.R
\name{opts_fs_perms}
\alias{opts_fs_perms}
\title{Constructive options for class 'fs_perms'}
\usage{
opts_fs_perms(constructor = c("as_fs_perms", "next"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object, see Details section.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_fs_perms>
}
\description{
These options will be used on objects of class 'fs_perms'.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"as_fs_perms"} (default): Build the object using \code{fs::as_fs_perms()} on
octal strings such as \code{"644"}. Vectors of length other than 1 or 2 are
first converted with \code{as.octmode()}, because \code{fs::as_fs_perms()} scrambles
the order of longer character vectors.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried.
}

Objects containing \code{NA} or negative values are always constructed with the
\code{"next"} constructor.
}
