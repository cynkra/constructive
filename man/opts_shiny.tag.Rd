% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-shiny.tag.R
\name{opts_shiny.tag}
\alias{opts_shiny.tag}
\title{Constructive options for class 'shiny.tag'}
\usage{
opts_shiny.tag(constructor = c("tags", "tag", "next"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_shiny.tag>
}
\description{
These options will be used on objects of class 'shiny.tag'.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"tags"} (default): We build the object using \verb{htmltools::tags$<name>()},
e.g. \code{htmltools::tags$div()}, with attributes as named arguments and children as
unnamed arguments. If the tag name is not a known tag or if some attributes
would clash with the formal arguments of the tag function, we fall back to
the \code{"tag"} constructor.
\item \code{"tag"} : We build the object using \code{htmltools::tag()}. \code{htmltools::tag()}
can't create an empty named list of attributes (as \code{htmltools::tags$div()}
does), in this case we fall back to the \code{"tags"} constructor for known tag
names, and to the list constructor otherwise.
\item \code{"next"} : Use the constructor for the next supported class.
}
}
