% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s4-denseMatrix.R
\name{opts_denseMatrix}
\alias{opts_denseMatrix}
\title{Constructive options for class 'denseMatrix'}
\usage{
opts_denseMatrix(constructor = c("Matrix", "next"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object, see Details section.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_denseMatrix>
}
\description{
These options will be used on objects of class 'denseMatrix' from the
'Matrix' package, i.e. objects of classes such as 'dgeMatrix', 'lgeMatrix',
'dsyMatrix' or 'dtrMatrix'.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"Matrix"} (default): We build the object using \code{Matrix::Matrix()} on a
base matrix, with \code{sparse = FALSE}. \code{Matrix::Matrix()} detects symmetric
and triangular matrices so if this cannot reproduce the object exactly
(e.g. a general matrix with symmetric content, packed storage, cached
factorizations) we fall back to the next constructor.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried.
}
}
