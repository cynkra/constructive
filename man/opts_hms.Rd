% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-hms.R
\name{opts_hms}
\alias{opts_hms}
\title{Constructive options for class 'hms'}
\usage{
opts_hms(constructor = c("as_hms", "hms", "new_hms", "next", "double"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object, see Details section.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_hms>
}
\description{
These options will be used on objects of class 'hms'.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"as_hms"} (default): We build the object using \code{hms::as_hms()} on a
character vector in the format \code{"12:34:56"}. If some values cannot be
reproduced exactly this way (e.g. negative values, durations of more than
24 hours, or some fractional seconds) we fall back to the \code{"hms"} constructor.
\item \code{"hms"} : We build the object using \code{hms::hms()} on \code{seconds}, \code{minutes}
and \code{hours} arguments. If the decomposition is not exact we provide
only the \code{seconds} argument.
\item \code{"new_hms"} : We build the object using \code{hms::new_hms()} on a double vector.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried.
\item \code{"double"} : We define as an atomic vector and repair attributes.
}
}
