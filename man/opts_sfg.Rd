% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-sfg.R
\name{opts_sfg}
\alias{opts_sfg}
\title{Constructive options for class 'sfg'}
\usage{
opts_sfg(constructor = c("st_sfg", "next"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object, see Details section.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_sfg>
}
\description{
These options will be used on objects of class 'sfg', simple feature
geometries from the 'sf' package.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"st_sfg"} (default): We build the object using the sf constructor
matching the geometry type, i.e. \code{sf::st_point()}, \code{sf::st_multipoint()},
\code{sf::st_linestring()}, \code{sf::st_multilinestring()}, \code{sf::st_polygon()},
\code{sf::st_multipolygon()} or \code{sf::st_geometrycollection()}, with a \code{dim}
argument when the dimension can't be inferred from the coordinates.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried.
}

Other geometry types, such as "CIRCULARSTRING", don't have a dedicated
constructor and are built with the next constructor.
}
