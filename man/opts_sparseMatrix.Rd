% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s4-sparseMatrix.R
\name{opts_sparseMatrix}
\alias{opts_sparseMatrix}
\title{Constructive options for class 'sparseMatrix'}
\usage{
opts_sparseMatrix(constructor = c("sparseMatrix", "next"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object, see Details section.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_sparseMatrix>
}
\description{
These options will be used on objects of class 'sparseMatrix' from the
'Matrix' package in compressed column, compressed row or triplet form, i.e.
objects of classes such as 'dgCMatrix', 'dgRMatrix', 'dgTMatrix',
'lgCMatrix', 'ngCMatrix', 'dsCMatrix' or 'dtCMatrix'.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"sparseMatrix"} (default): We build the object using
\code{Matrix::sparseMatrix()} on the (1-based) row and column indices of the
stored entries. If this cannot reproduce the object exactly (e.g. cached
factorizations, unit triangular matrices) we fall back to the next
constructor.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried.
}
}
