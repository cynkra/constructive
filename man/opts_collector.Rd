% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-collector.R
\name{opts_collector}
\alias{opts_collector}
\title{Constructive options for class 'collector'}
\usage{
opts_collector(constructor = c("col", "next", "list"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_collector>
}
\description{
These options will be used on objects of class 'collector', i.e. column
specifications as created by \code{readr::col_double()}, \code{readr::col_date()},
\code{readr::col_factor()} etc. They all share the class 'collector' and a subclass
'collector_\if{html}{\out{<type>}}' that maps to the constructor \verb{readr::col_<type>()}.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"col"} (default): We build the object using the relevant \verb{readr::col_*()}
function, e.g. \code{readr::col_double()} or \code{readr::col_date(format = "\%Y")},
providing only non default arguments.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried. This will usually
be equivalent to \code{"list"}
\item \code{"list"} : We define as a list and repair attributes
}
}
