% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-crs.R
\name{opts_crs}
\alias{opts_crs}
\title{Constructive options for class 'crs'}
\usage{
opts_crs(constructor = c("st_crs", "next"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object, see Details section.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_crs>
}
\description{
These options will be used on objects of class 'crs'.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"st_crs"} (default): We build the object using \code{sf::st_crs()} on an EPSG
code if possible (e.g. \code{sf::st_crs(4326)}), or else on the user input
(e.g. \code{sf::st_crs("OGC:CRS84")}) or on the wkt definition, the first that
reproduces the object exactly.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried.
}

The wkt definition of a crs depends on the installed PROJ version, so a crs
object created on a different setup might not be reproducible with
\code{sf::st_crs()}, in this case we fall back to the next constructor.
}
