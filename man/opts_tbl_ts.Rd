% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-tbl_ts.R
\name{opts_tbl_ts}
\alias{opts_tbl_ts}
\title{Constructive options for class 'tbl_ts'}
\usage{
opts_tbl_ts(constructor = c("tsibble", "as_tsibble", "next"), ...)
}
\arguments{
\item{constructor}{String. Name of the function used to construct the object, see Details section.}

\item{...}{Additional options used by user defined constructors through the \code{opts} object}
}
\value{
An object of class <constructive_options/constructive_options_tbl_ts>
}
\description{
These options will be used on objects of class 'tbl_ts', also known as tsibbles.
}
\details{
Depending on \code{constructor}, we construct the object as follows:
\itemize{
\item \code{"tsibble"} (default): Wrap the column definitions in a \code{tsibble::tsibble()}
call. If some column names conflict with the arguments of \code{tsibble::tsibble()}
we fall back to \code{"as_tsibble"}.
\item \code{"as_tsibble"} : Construct the underlying tibble and pipe it into
\code{tsibble::as_tsibble()}. Use \code{opts_tbl_df()} to tweak the construction
of the tibble.
\item \code{"next"} : Use the constructor for the next supported class. Call \code{.class2()}
on the object to see in which order the methods will be tried.
}

The "interval" attribute is always recomputed by 'tsibble', we only set
\code{regular = FALSE} for irregular tsibbles.
}
