# structure corner cases

    Code
      construct(a)
    Output
      c(name2 = 1) |>
        structure(levels = "label2") |>
        match.fun("attr<-")(".Names", "name1") |>
        match.fun("attr<-")(".Label", "label1")

